<h1>Public::Users#confirm</h1>
<p>Find me in app/views/public/users/confirm.html.erb</p>

<h2>本当に退会しますか？</h2>
<p>退会する場合は、「退会する」をクリックしてください。</p><br>

<%= link_to "退会しない", user_path(current_user) %>

<% if user_signed_in? && @user.id == current_user.id %>
<%= link_to "退会する", users_leave_path(current_user), method: :put, "data-confirm" => "本当に退会しますか？", class: "btn btn-outline-danger" %>
<% end %>