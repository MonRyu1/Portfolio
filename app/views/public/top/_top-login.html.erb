<div class="new-report-box">
	<span class="box-title">NEW !!</span>
	<ul>
		<% @reports.each do |report| %>
			<li class="new-report">
				
					<%= link_to report_path(report) do %>
						<%= attachment_image_tag report, :report_image, fallback: "no-image.png", :size => "200x200" %>
					<% end %>
				<div class="report-detail">
					<div><%= report.spot.name %></div>
					<div id="star-rate-<%= report.id %>"></div>
					<script>
						$('#star-rate-<%= report.id %>').raty({
							size      : 36,
							number    : 5,
							starOff   : '<%= asset_path('stars/star-off.png') %>',
							starOn    : '<%= asset_path('stars/star-on.png') %>',
							starHalf  : '<%= asset_path('stars/star-half.png') %>',
							half      : true,
							readOnly  : true,
							score     : <%= report.rate %>,
						});
					</script>
				</div>
				
			</li>
		<% end %>
	</ul>
</div>
<h3><%= link_to "波情報はこちら ＞", reports_path, class: "btn btn-default" %></h3>


<div class="new-forecast-box">
	<span class="box-title">NEW !!</span>
	<ul>
		<% @forecasts.each do |forecast| %>
			<li class="new-forecast">
				
				<%= link_to forecast_path(forecast) do %>
					<%= attachment_image_tag forecast.spot, :spot_image, fallback: "no-image.png", :size => "200x200" %>
				<% end %>
				<div class="report-detail">
					<div><%= forecast.spot.name %></div>
					<div id="star-rate2-<%= forecast.id %>"></div>
					<script>
						$('#star-rate2-<%= forecast.id %>').raty({
							size      : 36,
							numberMux : 5,
							starOff   : '<%= asset_path('stars/star-off.png') %>',
							starOn    : '<%= asset_path('stars/star-on.png') %>',
							starHalf  : '<%= asset_path('stars/star-half.png') %>',
							half      : true,
							readOnly  : true,
							score     : <%= forecast.rate %>,
						});
					</script>
				</div>
				
			</li>
		<% end %>
	</ul>
</div>
<h3><%= link_to "波予報はこちら ＞", forecasts_path, class: "btn btn-default" %></h3>
