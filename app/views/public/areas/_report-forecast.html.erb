<div class="area-show-report-container">
	<h2 class="area-show-title">過去の波情報</h2>
	<h4 class="link"><%= link_to "MORE >>", reports_path %></h4>
	<div class="new-report-box">
		<ul>
			<% area.spots.each do |spot| %>
				<% spot.reports.first(5).each do |report| %>
					<li class="new-report">

							<%= link_to report_path(report) do %>
								<%= attachment_image_tag report, :report_image, fallback: "no-image.png", :size => "120x120", class: "report-image" %>
							<% end %>
						<div class="report-detail">
							<div><%= report.spot.name %></div>
							<div id="star-rate-<%= report.id %>"></div>
							<script>
								$('#star-rate-<%= report.id %>').raty({
									size      : 36,
									number    : 5,
									starOff   : '<%= asset_path('stars/star-off.png') %>',
									starOn    : '<%= asset_path('stars/star-on.png') %>',
									starHalf  : '<%= asset_path('stars/star-half.png') %>',
									half      : true,
									readOnly  : true,
									score     : <%= report.rate %>,
								});
							</script>
						</div>

					</li>
				<% end %>
			<% end %>
		</ul>
	</div>
</div>
<script>
	$(function(){
	$('.report-image').mouseover(function(){
		$(this).addClass('action');
	});
	$('.report-image').mouseout(function(){
		$(this).removeClass('action');
	});
});
</script>

<div class="area-show-forecast-container">
	<h2 class="area-show-title">過去の波予報</h2>
	<h4 class="link"><%= link_to "MORE >>", forecasts_path %></h4>
	<div class="new-forecast-box">
		<ul>
			<% area.spots.each do |spot| %>
				<% spot.forecasts.first(5).each do |forecast| %>
					<li class="new-forecast">

						<%= link_to forecast_path(forecast) do %>
							<%= attachment_image_tag forecast.spot, :spot_image, fallback: "no-image.png", :size => "120x120", class: "forecast-image" %>
						<% end %>
						<div class="report-detail">
							<div><%= forecast.spot.name %></div>
							<div id="star-rate2-<%= forecast.id %>"></div>
							<script>
								$('#star-rate2-<%= forecast.id %>').raty({
									size      : 36,
									numberMux : 5,
									starOff   : '<%= asset_path('stars/star-off.png') %>',
									starOn    : '<%= asset_path('stars/star-on.png') %>',
									starHalf  : '<%= asset_path('stars/star-half.png') %>',
									half      : true,
									readOnly  : true,
									score     : <%= forecast.rate %>,
								});
							</script>
						</div>

					</li>
				<% end %>
			<% end %>
		</ul>
	</div>
</div>

<script>
	$(function(){
	$('.forecast-image').mouseover(function(){
		$(this).addClass('action');
	});
	$('.forecast-image').mouseout(function(){
		$(this).removeClass('action');
	});
});
</script>